package com.olmedilla

import java.io.File
import java.nio.file.{Path, Paths, Files, LinkOption}
import java.nio.file.attribute.BasicFileAttributes

object DeleteKubes  {
/*  import FileUtils._
  System.out.println("Operating System: "+os);
  if (args == null || args.size < 2) {
    System.out.println("Usage: <homes dir> <directory pattern>");
    System.exit(0)
  } else {
    System.out.println("Homes dir: "+args(0));
    System.out.println("Home pattern: "+args(1));
  }
  val baseDir = args(0);
  val pattern = args(1)
  val patterns = List(pattern);
  val homesFile = new File(baseDir);



  //val homes : List[File] = getDirectoriesByPrefix(homesFile,patterns)
  val homes = getPathsByPrefix(baseDir,pattern)
  System.out.println("Ficheros encontrados: ");

  //val directorios : Stream[Path] = getPathsByPrefix(baseDir, args(1))
  //val directorios = getPathsByPrefix(baseDir, args(1))
  homes.foreach(println(_))
  homes.foreach(deleteKubeDir(_))*/
}

